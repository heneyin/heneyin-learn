println "我在 ${project.name} 工程的 build.gradle 文件里"

tasks.register("subHello") {
    println "我在 ${project.name} 工程的 subHello task 里"
    doLast {
        println "我在 ${project.name} 工程的 subHello task 的 doLast 方法里"
    }
}