/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn more about Gradle by exploring our samples at https://docs.gradle.org/7.1.1/samples
 */

println "我在 root 工程的 build.gradle 里"

tasks.register("hello") {
    println "我在 root 工程的 hello task 里"
    doLast {
        println "我在 root 工程的 hello task 的 doLast 里"
    }
}

class GreetingPlugin implements Plugin<Project> {
    void apply(Project project) {
        project.task('helloPlugin') {
            doLast {
                println 'Hello from the GreetingPlugin'
            }
        }
    }
}

apply plugin: GreetingPlugin