/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn more about Gradle by exploring our samples at https://docs.gradle.org/7.1.1/samples
 */
defaultTasks 'hello'
println "我在 build.gradle 下"

tasks.register("world") {
    println "我在 build.gradle 下的 world task 中"
    doFirst {
        println "我在 build.gradle 下的 world task 中的 doFirst 中"
    }
    doLast {
        println "我在 build.gradle 下的 world task 中的 doLast 中"
    }
}

tasks.register("hello") {
    dependsOn 'world'
    println "我在 build.gradle 下的 hello task 中"
    doFirst {
        println "我在 build.gradle 下的 hello task 中的 doFirst 中"
    }
    doLast {
        println "我在 build.gradle 下的 hello task 中的 doLast 中"
    }
}

tasks.named('hello') {
    doFirst {
        println 'Hello World'
    }
}

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath group: 'com.google.guava', name: 'guava', version: '30.1.1-jre'
    }
}

tasks.register('guava') {
    doLast {
        String result = com.google.common.base.Preconditions.checkNotNull("123")
        println result
    }
}